server:
  port: 8083

spring:
  application:
    name: notification-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  simple:
    metrics:
      export:
        enabled: true
